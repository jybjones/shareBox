extends ../layouts/default

block content
  .container
      // HEADER
      .jumbotron.text-center
        h1 Welcome to Sh@reBox!
        a.button.btn.btn-primary(href='/auth/facebook') Log In With Facebook
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        a.button.btn.btn-primary(href='/auth/twitter') Log In With Twitter

    .span6
      if message && message.length
        .fade.in.alert.alert-error
          button.close(type="button", data-dismiss="alert") &times;
          strong #{message}
      block auth
