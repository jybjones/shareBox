extends ../layouts/default

block content
  .row
    .span6
      a.button.btn.btn-primary(href="/auth/facebook") Sign up with Facebook
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
      a.button.btn.btn-primary(href="/auth/twitter") &nbsp;&nbsp;Sign in with Twitter

    .span6
      if message && message.length
        .fade.in.alert.alert-error
          button.close(type="button", data-dismiss="alert") &times;
          strong #{message}
      block auth
